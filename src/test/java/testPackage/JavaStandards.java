package testPackage;

public class JavaStandards {
// To format the code on eclipse ctrl+shift+F
//Naming conventions in defining class, variable and methods use CAMEL CASE
//variable name , int rowCount, class name Kapil
//How to debug code in eclipse?
// Apply Toggle Breakpoint if you want to execute ur code line by line, so apply breakpoint n from there script will be running line by line 
// To run use run in debug mode by command "Debug As"
// after running script n page loading each steps / line on eclipse will be highlighted in GREEN if there is no error.
// STEP OVER / F6 : click on this to execute each step line by line, it will execute one line n then jump to next line n then execute it.
	
	
}
