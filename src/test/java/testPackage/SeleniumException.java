package testPackage;

public class SeleniumException {

	// No such element found = we checked that webelement is locating properly when we locate it via chropath or selector hub there it is highligting but wen script runs it throw exception then it means  it is timeout issue we 've to use implicit/explicit timeout.
//Element click Intercepted exception: test case 5th this last step : this exception may occur due to timeout
	//driver.findElement(By.className("reset-pwd-btn")).click();





}
